#include <bits/stdc++.h>

#define int long long

using namespace std;

void solve(){
    int n, m;
    cin >> n >> m;
    vector<int> price(n), weight(n), pw(n);
    for(int i = 0; i < n; i++){
        cin >> price[i] >> weight[i] >> pw[i];
    } 
}
int32_t main (){
    ios_base::sync_with_stdio(false);
    cin.tie(nullptr);
    int t;
    cin >> t;
    for(int i = 1; i <= t; i++) {
        solve();
    }
}